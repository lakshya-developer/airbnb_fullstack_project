<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Details</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap");

      body {
        font-family: "Inter", sans-serif;
      }

      .gradient-text {
        background: linear-gradient(45deg, #ff385c, #ff5a5f, #ff7875);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .glass-effect {
        background: rgba(0, 0, 0, 0.95);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
      }

      .logo-glow {
        filter: drop-shadow(0 0 8px rgba(255, 56, 92, 0.3));
      }

      .back-button {
        position: relative;
        overflow: hidden;
      }

      .back-button::before {
        content: "";
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(
          90deg,
          transparent,
          rgba(255, 255, 255, 0.1),
          transparent
        );
        transition: left 0.5s;
      }

      .back-button:hover::before {
        left: 100%;
      }

      .back-arrow {
        transition: transform 0.3s ease;
      }

      .back-button:hover .back-arrow {
        transform: translateX(-3px);
      }

      @keyframes float {
        0%,
        100% {
          transform: translateY(0px);
        }
        50% {
          transform: translateY(-3px);
        }
      }

      .float-animation {
        animation: float 3s ease-in-out infinite;
      }
    </style>
  </head>
  <body class="bg-gray-100 min-h-screen flex flex-col">
    <header class="glass-effect py-6 shadow-2xl border-b border-gray-700/50">
  <div class="container mx-auto px-6 flex justify-between items-center">
    <h1 class="text-white text-3xl font-bold float-animation">
      <a href="/" class="group">
        <span class="gradient-text logo-glow group-hover:scale-105 transition-transform duration-300">
          Airbnb
        </span>
      </a>
    </h1>
    <a href="<%= from %>" class="back-button text-white hover:text-gray-300 transition-all duration-300 px-4 py-2 rounded-lg hover:bg-gray-800/50 hover:shadow-lg group">
      <span class="back-arrow inline-block">←</span> Back to <%= from %>
    </a>
  </div>
</header>

    <main
      class="flex flex-grow justify-center items-center container mx-auto px-4 py-8"
    >
      <div
        class="bg-white rounded-2xl shadow-2xl overflow-hidden max-w-5xl mx-auto w-full"
      >
        <!-- Hero Image Section -->
        <div class="relative h-96 overflow-hidden">
          <img
            class="w-full h-full object-cover"
            src="<%= home.photo %>"
            alt="<%= home.houseName %>"
          />
          <div
            class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"
          ></div>
          <div
            class="absolute bottom-6 right-6 bg-white/95 backdrop-blur-sm rounded-full px-4 py-2 flex items-center gap-2 shadow-lg"
          >
            <svg
              class="h-5 w-5 text-yellow-500 fill-current"
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 200 200"
            >
              <polygon
                points="100,10 40,198 190,78 10,78 160,198"
                class="stroke-yellow-500"
                style="stroke-width: 5"
              />
            </svg>
            <span class="text-lg font-bold text-gray-800"
              ><%= home.rating %></span
            >
          </div>
        </div>

        <!-- Content Section -->
        <div class="p-8 lg:p-12">
          <!-- Title and Description -->
          <div class="mb-8">
            <h1
              class="text-4xl lg:text-5xl font-bold text-gray-900 mb-4 leading-tight"
            >
              <%= home.houseName %>
            </h1>
            <p class="text-lg text-gray-600 leading-relaxed max-w-3xl">
              <%= home.description %>
            </p>
          </div>

          <div class="mt-4">
            <a
              href="/rules/<%= home._id %>"
              class="inline-flex items-center text-blue-600 hover:text-blue-800"
            >
              <svg
                class="w-5 h-5 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 10v6m0 0l-3-3m3 3l3-3m2-8H7a2 2 0 00-2 2v5a2 2 0 002 2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 001.707-.707V19a2 2 0 00-2-2z"
                />
              </svg>
              Download House Rules
            </a>
          </div>

          <!-- Details Grid -->
          <div
            class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8"
          >
            <!-- Price Card -->
            <div
              class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl p-6 border border-blue-200"
            >
              <div class="flex items-center gap-3 mb-2">
                <svg
                  class="h-6 w-6 text-blue-600"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"
                  />
                </svg>
                <h3 class="text-lg font-semibold text-gray-800">
                  Price per Night
                </h3>
              </div>
              <p class="text-2xl font-bold text-blue-600">₹<%= home.price %></p>
            </div>

            <!-- Location Card -->
            <div
              class="bg-gradient-to-br from-green-50 to-green-100 rounded-xl p-6 border border-green-200"
            >
              <div class="flex items-center gap-3 mb-2">
                <svg
                  class="h-6 w-6 text-green-600"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
                  />
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"
                  />
                </svg>
                <h3 class="text-lg font-semibold text-gray-800">Location</h3>
              </div>
              <p class="text-lg font-medium text-green-700">
                <%= home.location %>
              </p>
            </div>

            <!-- Rating Card -->
            <div
              class="bg-gradient-to-br from-yellow-50 to-yellow-100 rounded-xl p-6 border border-yellow-200"
            >
              <div class="flex items-center gap-3 mb-2">
                <svg
                  class="h-6 w-6 text-yellow-600 fill-current"
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 200 200"
                >
                  <polygon
                    points="100,10 40,198 190,78 10,78 160,198"
                    class="stroke-yellow-600"
                    style="stroke-width: 5"
                  />
                </svg>
                <h3 class="text-lg font-semibold text-gray-800">Rating</h3>
              </div>
              <p class="text-lg font-medium text-yellow-700">
                <%= home.rating %> / 5
              </p>
            </div>
          </div>

          <!-- Action Buttons -->
          <% if (userType === "guest") { %>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <form action="/bookings" method="POST" class="flex-1 sm:flex-none">
              <input type="hidden" name="hid" value="<%= home._id %>" />
              <button
                class="w-full sm:w-auto bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white font-semibold px-8 py-4 rounded-xl transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 shadow-lg hover:shadow-xl flex items-center justify-center gap-3"
              >
                <svg
                  class="h-5 w-5"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
                  />
                </svg>
                Book Now
              </button>
            </form>

            <form action="/favourite" method="POST" class="flex-1 sm:flex-none">
              <input type="hidden" name="hid" value="<%= home._id %>" />
              <button
                class="w-full sm:w-auto bg-gradient-to-r from-pink-500 to-pink-600 hover:from-pink-600 hover:to-pink-700 text-white font-semibold px-8 py-4 rounded-xl transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-pink-500 focus:ring-offset-2 shadow-lg hover:shadow-xl flex items-center justify-center gap-3"
              >
                <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 20 20">
                  <path
                    fill-rule="evenodd"
                    d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z"
                    clip-rule="evenodd"
                  />
                </svg>
                Add to Favourites
              </button>
            </form>
          </div>
          <% } %>
        </div>
      </div>
    </main>

    <footer class="bg-gray-800 text-white py-4 text-center">
      <p>&copy; 2024 Airbnb. All rights reserved.</p>
    </footer>
  </body>
</html>
